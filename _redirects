# Cloudflare — przekierowania (działają zarówno w Pages jak i Workers Static Assets)

# Ładny URL panelu admina (bez końcowego /index.html)
/admin   /admin/index.html   200

# Uwaga: 404 jest obsługiwany przez "not_found_handling": "404-page" w wrangler.jsonc.
# Nie dajemy tutaj /* → 404, bo to złapałoby też istniejące strony.
